# Technologies for Artificial Intelligence — course environment
#
# Deliberately free of any LLM stack: this course covers ML fundamentals and
# nothing here should require an API key or a network call at runtime.

# ============================
# Core scientific stack
# ============================
numpy
pandas
scipy
matplotlib
seaborn

# ============================
# Classical machine learning
# ============================
scikit-learn
statsmodels
# xgboost-cpu for the same reason as tensorflow-cpu below: the default xgboost
# wheel depends on nvidia-nccl-cu12, another ~450 MB of CUDA nobody here uses.
# Identical API, published by the same project.
xgboost-cpu

# ============================
# Deep learning (CPU only)
# ============================
# tensorflow-cpu, not tensorflow: the plain package pulls in the nvidia-* CUDA
# libraries as hard dependencies, roughly 450 MB of them, even on a machine
# with no GPU. Every lab in this course runs on CPU by design, so that is pure
# download cost for every student.
tensorflow-cpu
tensorboard

# ============================
# Datasets
# ============================
# Fetches the openml / UCI datasets used in the labs, with local caching so a
# lab can be re-run offline once the data has been downloaded at least once.
pooch

# ============================
# Jupyter environment
# ============================
jupyterlab>=4.2,<5
notebook
ipykernel
ipywidgets>=8.1
ipympl
jupyterlab-code-formatter
jupyterlab-git
